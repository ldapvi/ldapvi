[package]
name = "test-driver"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
nix = { version = "0.29", features = ["term", "socket", "process", "fs"] }

[dev-dependencies]
tempfile = "3"
